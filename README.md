# encryption-algorithm
this encryption algorithm will help  u to shuffle there bit postion with respect to their original values.

this is an AES algorithm where is repeating cycle of steps are unique for each set of words. for increasing there complexity to creak,
are backtrack.
